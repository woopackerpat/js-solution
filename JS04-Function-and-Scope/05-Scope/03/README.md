ผลลัพธ์ในบรรทัดที่มี \* มีค่าเป็นอะไรและเพราะอะไร

```js
let phrase = "Hello";
if (true) {
  let user = "John";
  // const sayHi = () => {
  //   alert("");
  // };
  function sayHi() {
    alert(`${phrase}, ${user}`);
  }
}
sayHi(); // * Hello John
```
